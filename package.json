{
  "name": "template",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "postcss": "postcss src/css/*.css --dir dist/css/ --verbose",
    "dev": "NODE_ENV=dev npm run postcss && eleventy --serve",
    "build": "NODE_ENV=prod npm run postcss && eleventy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "tailwindcss": "^1.1.3"
  }
}
